name: Deploy NPM Project via SSH

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      HOST: 5.196.64.87
      USERNAME: ${{ secrets.USERNAME }}
      PASSWORD: ${{ secrets.PASSWORD }}

    steps:
    - name: SSH to server and start application
      uses: appleboy/ssh-action@v0.1.0
      with:
        host: ${{ env.HOST }}
        username: ${{ env.USERNAME }}
        password: ${{ env.PASSWORD }}
        script: |
          cd /home/gacv2/green-art-frontend && \
          /home/gacv2/.nvm/versions/node/v20.18.0/bin/npm run build && \
          /home/gacv2/.nvm/versions/node/v20.18.0/bin/npm run start &
